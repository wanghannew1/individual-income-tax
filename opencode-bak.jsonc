{
  "$schema": "https://raw.githubusercontent.com/opencode-ai/opencode/main/opencode.schema.json",
  "projectRules": [
    "所有任务必须只用主代理（Sisyphus）直接执行，禁止使用任何子代理（sub-agent）进行任务委派。",
    "禁止调用 task() 来创建子任务或委派工作给其他代理。",
    "所有代码修改、搜索、分析等工作都必须由主代理通过直接工具调用完成。"
  ]
}
